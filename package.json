{
    "name": "@local/moonshine-pdf-viewer",
    "version": "0.1.0",
    "private": true,
    "description": "Standalone frontend build for Laravel PDF viewer package",
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite build --watch",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "pdfjs-dist": "^5.4.624"
    },
    "devDependencies": {
        "vite": "^7.0.4"
    }
}
